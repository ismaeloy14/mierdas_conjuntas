//5. Heu de crear un script que declari tres variables de tipus numèric i desprès faci diferents càlculs
//aritmètics entre elles (suma, resta, divisió, mòdul, multiplicació), i emagatzemar el resultat en 5
//variables. Desprès de l'operació de suma i resta poseu un alert que digui breakpoint i feu servir la
//consola per consultar els valors de les diferents variables. Cal posar comentaris al codi.

//es declaren les variables
var a=4, b=7;
//Es guarden les operacions en cinc variables diferents
var sum = a+b;
var resta = a-b;
//Es crea un alert per què pari l'execució del programa fins que es premi acceptar
alert("breakpoint");
var divisio = a/b;
var modul = a%b;
var mult = a*b;

//Faig un console.log per veure el que tinc a una variable

console.log(divisio);